import { jsx as _jsx } from "react/jsx-runtime";
import { StatsDisplay } from './StatsDisplay.js';
import { useSessionStats } from '../contexts/SessionContext.js';
import { escapeShellArg, getShellConfiguration } from '@google/gemini-cli-core';
export const SessionSummaryDisplay = ({ duration, }) => {
    const { stats } = useSessionStats();
    const { shell } = getShellConfiguration();
    const footer = `To resume this session: gemini --resume ${escapeShellArg(stats.sessionId, shell)}`;
    return (_jsx(StatsDisplay, { title: "Agent powering down. Goodbye!", duration: duration, footer: footer }));
};
//# sourceMappingURL=SessionSummaryDisplay.js.map