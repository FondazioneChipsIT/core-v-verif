import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
import { useMemo } from 'react';
import { Box, Text } from 'ink';
import { theme } from '../semantic-colors.js';
import { UserAccountManager, AuthType, } from '@google/gemini-cli-core';
export const UserIdentity = ({ config }) => {
    const authType = config.getContentGeneratorConfig()?.authType;
    const email = useMemo(() => {
        if (authType) {
            const userAccountManager = new UserAccountManager();
            return userAccountManager.getCachedGoogleAccount() ?? undefined;
        }
        return undefined;
    }, [authType]);
    const tierName = useMemo(() => (authType ? config.getUserTierName() : undefined), [config, authType]);
    if (!authType) {
        return null;
    }
    return (_jsxs(Box, { flexDirection: "column", children: [_jsxs(Box, { children: [_jsx(Text, { color: theme.text.primary, wrap: "truncate-end", children: authType === AuthType.LOGIN_WITH_GOOGLE ? (_jsxs(Text, { children: [_jsxs(Text, { bold: true, children: ["Signed in with Google", email ? ':' : ''] }), email ? ` ${email}` : ''] })) : (`Authenticated with ${authType}`) }), _jsx(Text, { color: theme.text.secondary, children: " /auth" })] }), tierName && (_jsxs(Box, { children: [_jsxs(Text, { color: theme.text.primary, wrap: "truncate-end", children: [_jsx(Text, { bold: true, children: "Plan:" }), " ", tierName] }), _jsx(Text, { color: theme.text.secondary, children: " /upgrade" })] }))] }));
};
//# sourceMappingURL=UserIdentity.js.map