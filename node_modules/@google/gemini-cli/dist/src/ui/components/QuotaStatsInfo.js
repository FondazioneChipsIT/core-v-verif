import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { Box, Text } from 'ink';
import { theme } from '../semantic-colors.js';
import { formatResetTime } from '../utils/formatters.js';
import { getUsedStatusColor, QUOTA_USED_WARNING_THRESHOLD, QUOTA_USED_CRITICAL_THRESHOLD, } from '../utils/displayUtils.js';
export const QuotaStatsInfo = ({ remaining, limit, resetTime, showDetails = true, }) => {
    if (remaining === undefined || limit === undefined || limit === 0) {
        return null;
    }
    const usedPercentage = 100 - (remaining / limit) * 100;
    const color = getUsedStatusColor(usedPercentage, {
        warning: QUOTA_USED_WARNING_THRESHOLD,
        critical: QUOTA_USED_CRITICAL_THRESHOLD,
    });
    return (_jsxs(Box, { flexDirection: "column", marginTop: 0, marginBottom: 0, children: [_jsx(Text, { color: color, children: remaining === 0
                    ? `Limit reached${resetTime
                        ? `, resets in ${formatResetTime(resetTime, 'terse')}`
                        : ''}`
                    : `${usedPercentage.toFixed(0)}% used${resetTime
                        ? ` (Limit resets in ${formatResetTime(resetTime, 'terse')})`
                        : ''}` }), showDetails && (_jsxs(_Fragment, { children: [_jsxs(Text, { color: theme.text.primary, children: ["Usage limit: ", limit.toLocaleString()] }), _jsx(Text, { color: theme.text.primary, children: "Usage limits span all sessions and reset daily." }), remaining === 0 && (_jsx(Text, { color: theme.text.primary, children: "Please /auth to upgrade or switch to an API key to continue." }))] }))] }));
};
//# sourceMappingURL=QuotaStatsInfo.js.map